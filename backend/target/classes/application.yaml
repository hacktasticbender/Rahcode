spring:
  application:
    name: ticketing
  datasource:
    url: jdbc:postgresql://localhost:5432/ticketing
    username: ticket
    password: ticket
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
  flyway:
    enabled: true
    locations: classpath:db/migration
server:
  port: 8080
app:
  jwt:
    # Base64-encoded 256-bit secret (change in real deployments)
    secret: c2VjdXJlLXNlY3JldC1zZWN1cmUtc2VjdXJlLXNlY3JldC1zZWN1cmU=
    expirationMs: 86400000
